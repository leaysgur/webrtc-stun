{
  "name": "webrtc-stun",
  "version": "0.0.0",
  "description": "",
  "devDependencies": {
    "@types/node": "^10.12.12",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.2.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "tslint -p . --fix && tsc -p . --noEmit"
  },
  "keywords": ["webrtc", "nodejs", "stun"],
  "author": "leader22",
  "license": "MIT"
}
